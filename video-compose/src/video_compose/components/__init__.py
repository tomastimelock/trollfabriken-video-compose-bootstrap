# Bundled HTML component templates — rendered via web-overlay (Playwright).
# Each .html file uses {{prop_name}} placeholders substituted by ComponentOverlay.props.
# Background must be pure black (#000) — compositor removes it via colorkey.
